基于需求文档生成以下数据并整理为word

1、API名称：金石智联-接口名称
2、APICODE：open.api.supplier.jszj.接口行为
3、请求JSON样例、返回JSON样例，必须包含所有字段
4、请求数据说明和返回数据说明（按照表格列出来）：字段名  字段中文名 
比如：mobile 手机号  

每个接口按以上顺序列出来，后续要复制

1、明确需求文档中需要对接的接口 
2、从客户文档中找到对应的接口 
3、根据接口生成以下模块
3.1、拿到客户入参、生成样例json
3.2、拿到客户入参、入参说明 生成表格两列，分别是字段和入参说明
3.3、拿到客户出参、生成样例json
3.4、拿到客户出参、出参说明 生成表格两列，分别是字段和出参说明
3.5、生成apicode：open.api.supplier.{{客户标识}}.接口行为
3.6、根据文档拿到对应的接口链接（如果主调域名都是相同，则只需要取历史）

模版：
apicode: open.api.supplier.jszy.updateDriverDoc

接口链接：/user/updateDriverDoc

入参示例：
{
    "driverFrontFileUrl": "RW260706-000414",
   	  "source": "DP",
}

入参说明（表格）：
|driverFrontFileUrl|驾驶证主页图片URL|
|source|来源|

出参示例：
{
    "code": "10000",
   	  "msg": "成功",
}

出参说明（表格）：
|code|响应码|
|msg|响应信息|

输出一个word文档
